% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filesystem-functions.R
\name{check_projectmanagr_file}
\alias{check_projectmanagr_file}
\title{Determine the type of a single projectmanagr file}
\usage{
check_projectmanagr_file(filePath, orgPath, settings)
}
\arguments{
\item{filePath}{Absolute path to a file within the organisation.}

\item{orgPath}{Absolute path to the root of the projectmanagr organisation.}

\item{settings}{A list of organisation settings, as loaded by
\code{\link[=get_settings_yml]{get_settings_yml()}}. Expected to include file naming keys such as
\code{"OrgIndexFileNamePrefix"}, \code{"ProgIndexFileNamePrefix"},
\code{"ProjectPrefixSep"}, \code{"ProjectIndexSep"}, etc.}
}
\value{
A character string among \code{"ORG"}, \code{"PROG"}, \code{"DOC"},
\code{"NOTE"}, \code{"HEAD"}, \code{"SUB"}, or \code{"UNKNOWN"}.
}
\description{
Checks whether a file is the Organisation index file, a Programme index file,
or else defers to \code{\link[=get_file_type]{get_file_type()}} to classify it as "DOC", "NOTE", "HEAD",
"SUB", or "UNKNOWN".
}
\details{
Specifically:
\enumerate{
\item If \code{filePath} is exactly the organisation index file (as returned
by \code{\link[=get_index_org]{get_index_org()}}), returns \code{"ORG"}.
\item If \code{filePath} is exactly a programme index file (as returned by
\code{\link[=get_index_prog]{get_index_prog()}} for that directory), returns \code{"PROG"}.
\item Otherwise, calls \code{\link[=get_file_type]{get_file_type()}} to see if the file is a
project doc (\code{"DOC"}), a note (\code{"NOTE"}), a header note
(\code{"HEAD"}), a sub-note (\code{"SUB"}), or \code{"UNKNOWN"}.
}
}
\examples{
\dontrun{
  orgPath <- "/path/to/myOrg"
  filePath <- "/path/to/myOrg/prog001~_Example.Rmd"
  # Suppose 'settings' was loaded via get_settings_yml(orgPath)
  check_projectmanagr_file(filePath, orgPath, settings)
  # returns "DOC" or "NOTE", etc., if recognized
}

}
\seealso{
\code{\link[=get_index_org]{get_index_org()}}, \code{\link[=get_index_prog]{get_index_prog()}}, \code{\link[=get_file_type]{get_file_type()}}
}
