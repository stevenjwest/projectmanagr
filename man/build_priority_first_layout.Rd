% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-functions.R
\name{build_priority_first_layout}
\alias{build_priority_first_layout}
\title{Build the final output with priority-first layout}
\usage{
build_priority_first_layout(
  allTodos,
  invMap,
  itemLines,
  noteLines,
  prioritySectionLines,
  fromFilePath,
  orgPath,
  date,
  location,
  collectionLines
)
}
\arguments{
\item{allTodos}{The data frame of all collected TODO items.}

\item{invMap}{A named character vector inverting numeric -> priority name,
e.g. \code{c("4"="DEADLINE_PASSED", "3"="SCHEDULE_PASSED", ...)}.}

\item{itemLines}{The lines from \code{itemTemplate}.}

\item{noteLines}{The lines from \code{noteTemplate}.}

\item{prioritySectionLines}{The lines from \code{priorityTemplate}.}

\item{fromFilePath}{Path from which links are computed.}

\item{orgPath}{Path to the project root for relative references.}

\item{date}{The user's "today" date string.}

\item{location}{The original location passed to \code{extract_todos()}.}

\item{collectionLines}{The lines from \code{collectionTemplate}.}
}
\value{
A character vector of the final doc in Rmd form.
}
\description{
In "priority-first" mode, we group items by priority level (descending).
Each priority becomes a top-level heading (expanded in the
\code{prioritySectionLines}), and under it we list files (in descending mod
time), and under each file, the items that match that priority.
}
\keyword{internal}
